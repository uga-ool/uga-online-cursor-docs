---
description: Keep OOL upstream/template repos read-only during app work
alwaysApply: true
---

# OOL upstream reference repos

Multi-root workspace folder names below match the team layout (sibling repos, e.g. under `local_dev/`).

## Repo tiers

| Tier | Folder name | Agent behavior |
|------|-------------|----------------|
| **Strict reference** | `UGA-Brightspace-React-Template` | Do not edit unless the user explicitly targets this repo |
| **Canonical upstream** | `uga-online-brightspace-agent-framework`, `uga-lit-components` | Read-only during app/agent work; no drive-by fixes; upstream changes only via intentional PRs |
| **Work repos** | `UGA-Brightspace-React-Apps`, `agent-todd`, `uga-drive-elc-sync`, `uga-timeline-js-aws`, `uga-elc-kaltura-caption-import` | Normal edits |
| **Docs hub** | `uga-online-cursor-docs` | Rules and team docs only; do not paste full upstream code here |

## Before editing

1. Check the path or workspace folder name against the table above.
2. If the user asks to build or change an **app feature**, implement in a **work repo**. For **new** course-file React apps, you **must** use `UGA-Brightspace-React-Apps/apps/<kebab-case-name>/`.
3. If the change belongs only in a reference or canonical upstream repo, **stop** and tell the user to open an upstream PR to `uga-ool`, or use the **scaffold-from-ool-template** skill to copy patterns into a work repo.

## Scaffolding new apps

Do not scaffold by editing `UGA-Brightspace-React-Template` in place. Read the template; write the app under a work repo. Follow the **scaffold-from-ool-template** skill.

## New course-file React apps

- Any **new** Brightspace course-file React app **must** live under **`UGA-Brightspace-React-Apps/apps/<kebab-case-name>/`** (one standalone npm package per folder; see that repo’s README).
- Do not create new course-file React apps as sibling repos or only inside `UGA-Brightspace-React-Template`.
- **Exception:** Legacy standalone repos in the workspace (e.g. `uga-elc-kaltura-caption-import`) — extend in place only when the user explicitly names that repo; do not use them as the default for greenfield apps.

## Related rules

- Course-file React conventions: `uga-brightspace-react-template.mdc`
- eLC AI agent plugins: `elc-ai-agent-tool.mdc`
- OOL GitHub practices: `uga-ool-github.mdc`

## Limits

Rules are advisory. Run `git status` on reference clones before committing. Prefer a work-focused workspace when using Agent mode on app tasks.
